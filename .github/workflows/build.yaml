name: build-test
run-name: ${{ github.actor }} initiated build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./build.sh -S
      - run: ./build.sh -d

